class ResetPasswordScreen : Screen<ResetPasswordScreen>() {

    val descriptionText = KTextView { withId(R.id.descriptionTextView) }
    val emailAddressField = KEditText { withId(R.id.emailAddressField) }
    val resetPasswordButton = KButton { withId(R.id.sendButton) }
    val returnButton = KButton { withId(R.id.returnButton) }
}
